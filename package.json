{
  "name": "eleventy-tailwind-starter",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "rm -rf _site && npx @11ty/eleventy --serve --quiet & npx tailwindcss -i ./src/css/base.css -o ./_site/css/styles.css --watch",
    "build": "rm -rf _site && ELEVENTY_ENV=production npx @11ty/eleventy && npx tailwindcss -i ./src/css/base.css -o ./_site/css/styles.css --minify"
  },
  "keywords": [],
  "author": "Adam Mish",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "prettier": "^3.0.2",
    "prettier-plugin-tailwindcss": "^0.5.3",
    "tailwindcss": "^3.3.3"
  }
}
