{
  "name": "eleventy-tailwind-starter",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "rm -rf _site && npx @11ty/eleventy --serve & npx @tailwindcss/cli -i ./src/css/base.css -o ./_site/css/styles.css --watch",
    "build": "rm -rf _site && ELEVENTY_ENV=production npx @11ty/eleventy && npx @tailwindcss/cli -i ./src/css/base.css -o ./_site/css/styles.css --minify"
  },
  "keywords": [],
  "author": "Adam Mish",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@tailwindcss/cli": "^4.1.17",
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.7.1",
    "tailwindcss": "^4.1.17"
  }
}
